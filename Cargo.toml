[workspace]
resolver = "2"
members = [
    "dartfrog",
    "dartfrog_lib",
    "chat",
    "piano",
    "page",
    "chess",
    "inbox",
]

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
